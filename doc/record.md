# 修改记录

## 2026-07-18 10:57:10 CST

- 修改人：codex
- 针对问题：项目缺少发票与支付截图整理软件的需求分析和依赖分析文档。
- 修改内容：新增 `doc/requirements-and-dependencies.md`，记录项目范围、功能优先级、界面需求、数据模型、待确认核算规则、软件架构、本地存储、依赖选择、构建环境、风险、测试及验收标准。

## 2026-07-18 11:01:04 CST

- 修改人：codex
- 针对问题：导出文件命名规则与实际要求不一致。
- 修改内容：将发票和支付截图的导出名称统一调整为 `001_物品名称.原扩展名`，并补充序号生成、非法字符清理和长度限制规则。

## 2026-07-18 11:20:56 CST

- 修改人：codex
- 针对问题：需求文档中的 Mermaid 架构代码无法直接查看图形。
- 修改内容：新增 `doc/assets/software-architecture.svg` 可视化架构图，并将需求文档中的 Mermaid 代码替换为可直接渲染的图片。

## 2026-07-18 11:28:13 CST

- 修改人：codex
- 针对问题：需求文档没有明确说明 Windows 安装后的本地存储位置、数据生命周期和保存恢复逻辑。
- 修改内容：补充 Windows 程序目录、应用配置、临时数据、项目目录和导出目录映射，并明确项目创建、打开、附件导入、原子保存、备份恢复、删除、升级及卸载保留规则。

## 2026-07-18 11:33:44 CST

- 修改人：codex
- 针对问题：原需求允许无发票明细，与“明细必须有发票、支付截图可选”的业务规则冲突。
- 修改内容：删除无发票核算和无发票导出逻辑，规定每条正式明细至少关联一张发票，未关联发票的待完善行不得核算或导出，支付截图缺失时允许手工录入实际付款金额。

## 2026-07-18 11:35:50 CST

- 修改人：codex
- 针对问题：上一轮将明细错误限制为必须有发票，与参考截图中的“有发票金额、无发票金额”核算规则冲突。
- 修改内容：回滚“必须有发票”约束，恢复明细可无发票、可无支付截图，并恢复无发票金额统计、核算和导出规则；同时恢复 `001_物品名称.原扩展名` 导出命名。

## 2026-07-18 11:39:41 CST

- 修改人：codex
- 针对问题：OCR 需求没有明确兼顾 Windows 部署便利性、识别准确率和系统资源占用的方案及门槛。
- 修改内容：将 PP-OCRv6 small + ONNX Runtime WASM 定为首选验证方案，规定纯离线单引擎部署、单 Worker、并发数 1、最多 2 个线程、延迟/内存/包体积/准确率门槛、模型切换规则和 Windows OCR 专项测试。

## 2026-07-18 12:29:11 CST

- 修改人：codex
- 针对问题：项目只有需求文档，没有可运行的软件初版。
- 修改内容：初始化 Electron、React、TypeScript、Vite 和 Fluent UI 工程；实现安全 IPC、本地项目创建/打开、项目锁、自动保存、滚动备份、发票与支付附件导入及 SHA-256 去重、报销明细编辑、类别与发票状态汇总、Excel/ZIP/manifest 导出、初版界面和使用说明；新增领域计算、项目存储和 ZIP 导出测试；通过类型检查、4项测试、生产构建和 Linux unpacked 打包验证，并将 ExcelJS 的 uuid 传递依赖覆盖到已修复版本 11.1.1。

## 2026-07-18 12:42:30 CST

- 修改人：codex
- 针对问题：开发模式下沙箱 preload 加载本地 `shared/models` 模块失败，导致 `window.invoiceManager` 未注册，打开项目时报 `Cannot read properties of undefined (reading 'openProject')`。
- 修改内容：将 preload 使用的 IPC 通道常量内联到单一 preload 文件，仅保留共享模型的类型导入，避免沙箱运行时加载本地模块，同时保留 `sandbox` 和 `contextIsolation` 安全配置。

## 2026-07-18 12:55:16 CST

- 修改人：codex
- 针对问题：点击“新建项目”无法输入项目名称，因为渲染端使用了 Electron 不支持的 `window.prompt()`；“添加类别”存在相同问题。
- 修改内容：使用 Fluent UI `Dialog`、`Field` 和 `Input` 实现项目名称与类别名称输入，增加空名称和重复类别校验，并移除两处 `window.prompt()` 调用；项目存储创建测试保持通过。

## 2026-07-18 13:14:32 CST

- 修改人：codex
- 针对问题：报销明细无法设置实际付款人，且未打开项目时标题栏与欢迎页重复显示新建、打开功能。
- 修改内容：为明细模型、录入表格和 Excel 导出增加“实际付款人”字段，旧项目读取时自动补为空值；未打开项目时隐藏标题栏项目工具栏，仅保留欢迎页的新建、打开入口；同步更新需求、README 和兼容性测试。

## 2026-07-18 13:24:10 CST

- 修改人：codex
- 针对问题：实际付款人应由应用全局配置；打开最近项目缺少候选列表；总价核算缺少付款人维度；原生菜单栏和页面自定义标题重复占用空间。
- 修改内容：新增持久化应用设置，统一保存付款人名单及最多 10 个最近项目；付款人改为全局名单选择，首页和项目工具栏展示最近项目候选并区分“从本地打开”；界面、Excel 和 manifest 增加按付款人汇总；移除原生菜单栏及页面内重复软件标题，保留系统标题栏；新增应用设置测试并更新需求与 README。

## 2026-07-18 13:26:28 CST

- 修改人：codex
- 针对问题：全局付款人设置入口被错误放在新建项目欢迎区和项目工具栏内，界面层级与全局配置的实际作用域不一致。
- 修改内容：从新建项目欢迎区及项目工具栏移除付款人设置按钮，在始终显示的全局状态栏中提供唯一的“全局付款人设置”入口；付款人名单仍保存在应用级 `settings.json` 并供所有项目共用。

## 2026-07-18 13:34:15 CST

- 修改人：codex
- 针对问题：发票与支付截图列位置不符合要求；付款人、类别和导出选项入口分散；导出 ZIP 包含 JSON 且完成后不能选择打开目录；最近项目切换后选择器仍显示占位文案。
- 修改内容：将发票、支付截图移动到操作列前的倒数第三、倒数第二列并删除原位置；统一“设置”界面，纳入全局付款人、支付截图导出选项和当前项目类别；移除 `manifest.json` 导出并在完成后弹窗询问是否打开目录；最近项目选择器绑定当前项目路径以显示项目名称；更新设置持久化、测试、需求和 README。

## 2026-07-18 13:38:13 CST

- 修改人：codex
- 针对问题：项目名称、绝对路径、状态提示和设置入口混排的整条状态栏不美观，用户要求不保留。
- 修改内容：删除状态栏及项目绝对路径展示；未打开项目时将设置作为右上角独立入口，打开项目后放入项目工具栏；只读和未保存标识移入项目工具栏；操作状态改为 3.5 秒后自动消失的底部浮层提示。

## 2026-07-18 13:45:25 CST

- 修改人：codex
- 针对问题：项目工具栏按钮右侧集中排列，不符合左对齐建议。
- 修改内容：将项目工具栏容器的水平对齐方式由右对齐调整为左对齐，其余按钮顺序和间距保持不变。

## 2026-07-18 13:50:14 CST

- 修改人：codex
- 针对问题：设置界面只能添加类别、不能删除类别，付款人移除也缺少引用检查。
- 修改内容：将类别和付款人统一改为可增删列表，变更仅在保存设置时生效；使用中的类别及最后一个类别不可删除；应用记录所有已打开项目路径，删除全局付款人前检查已知项目的 `project.json`，存在明细引用时拒绝删除并提示项目名称；新增跨项目付款人引用测试。

## 2026-07-18 13:56:18 CST

- 修改人：codex
- 针对问题：已有项目创建后无法修改项目名称。
- 修改内容：在统一设置中新增“当前项目名称”编辑项，禁止保存空名称；名称变更写入项目 `project.json`，同步更新最近项目候选及后续导出名称；为避免已有路径和外部引用失效，项目目录名称保持不变；补充项目名称持久化与最近项目名称同步测试。

## 2026-07-18 14:00:16 CST

- 修改人：codex
- 针对问题：“导出压缩包时包含支付截图”和附件重命名说明放在全局设置中，不符合导出操作的交互直觉。
- 修改内容：从应用设置模型、持久化文件和设置界面移除支付截图导出选项；点击“导出 ZIP”时先显示独立导出确认窗口，在其中选择是否包含支付截图并查看附件重命名说明，确认后再选择保存位置；同步更新测试和需求文档。

## 2026-07-18 14:04:23 CST

- 修改人：codex
- 针对问题：项目工具栏中的“从本地打开”按钮可与最近项目下拉菜单合并，减少分散入口。
- 修改内容：在最近项目下拉菜单中新增“从本地打开…”选项并复用原目录选择流程，删除工具栏旁边的独立“从本地打开…”按钮；首页入口保持不变。

## 2026-07-18 14:11:27 CST

- 修改人：codex
- 针对问题：项目切换下拉框在已打开项目时仍保留“打开最近项目”占位文字，信息重复。
- 修改内容：删除“打开最近项目”占位项，将下拉框无障碍名称改为“切换项目”，菜单仅保留“从本地打开…”及最近项目候选，当前选择继续显示项目名称。

## 2026-07-18 14:14:22 CST

- 修改人：codex
- 针对问题：已添加的发票和支付截图只能打开，无法删除。
- 修改内容：点击附件数量进入对应的发票或支付截图管理窗口，可逐个打开、删除或继续添加附件；删除时移除当前明细关联，无其他明细引用时同时移除项目附件元数据，共享附件仍被其他明细引用时保留；只读项目禁用删除和添加。

## 2026-07-18 14:17:09 CST

- 修改人：codex
- 针对问题：发票“添加”按钮左侧与“已报销”复选框距离过近，容易误触报销状态。
- 修改内容：在“已报销”和“发票”列之间增加双像素分隔线及左侧点击缓冲，限制复选框单元格溢出并固定最小宽度，同时提高附件操作区命中层级。

## 2026-07-18 14:21:26 CST

- 修改人：codex
- 针对问题：报销明细表列宽、行高和单元格间距不协调，整体视觉拥挤且各字段空间分配不合理。
- 修改内容：增加 `colgroup` 固定 13 列的语义宽度，扩大名称与备注列、收紧金额和状态列；统一表头高度、数据行高度、控件高度及单元格内边距；增加轻量隔行底色和悬停反馈，并调整附件操作间距。

## 2026-07-18 14:23:29 CST

- 修改人：codex
- 针对问题：日期和详细名称的 Fluent UI 输入框保留组件默认最小宽度，超出固定列宽后与相邻格串位。
- 修改内容：同时约束 `.fui-Input` 容器和内部原生输入框的宽度与最小宽度，使控件严格限制在所属表格单元格内。

## 2026-07-18 14:33:03 CST

- 修改人：codex
- 针对问题：项目已经默认自动保存，工具栏“保存”按钮功能重复。
- 修改内容：删除工具栏手动保存按钮；保留编辑停止 1 秒后的自动保存和导出前强制保存；移除仅供手动保存成功提示使用的函数参数及分支。

## 2026-07-18 14:41:05 CST

- 修改人：codex
- 针对问题：明细总价已有金额时，实际付款仍需重复手工录入，导致实际付款、有票、无票、已报销和付款人汇总错误显示为零。
- 修改内容：确认并固化“实际付款＝价格＋税费”规则；删除明细模型中的独立实际付款字段及表格手工输入，改为自动显示总价；汇总、付款人核算和 Excel 导出统一使用总价；有发票关联的整条明细计入有票金额，否则计入无票金额；更新旧数据兼容解析、测试、README 和需求文档。

## 2026-07-18 14:50:43 CST

- 修改人：codex
- 针对问题：现有界面颜色、圆角、阴影、间距和信息层级不统一，缺少 Windows 11 原生应用的视觉一致性。
- 修改内容：基于现有 Fluent UI 组件重构全局视觉样式；使用 Segoe UI Variable、Mica 式渐变与半透明背景、系统蓝强调色、轻量边框和低层级阴影；统一工具栏、欢迎页、表格、核算侧栏、设置列表、对话框、滚动条和响应式间距；保留既有交互、左对齐工具栏及表格列宽约束。

## 2026-07-18 15:00:44 CST

- 修改人：codex
- 针对问题：上一轮只对原生下拉框和按钮做 CSS 美化，无法确认所有交互控件均符合 Win11 Fluent 风格。
- 修改内容：将项目切换、类别和付款人原生下拉框替换为 Fluent `Select`；将最近项目与附件数量裸按钮替换为 Fluent `Button`；将两处 `window.confirm` 浏览器确认框替换为 Fluent `Dialog`；移除覆盖 Fluent 下拉和按钮外观的自绘 CSS，仅保留布局约束；日期继续由 Fluent `Input` 承载系统日期控件，文件选择继续使用 Windows 原生系统对话框。

## 2026-07-18 15:16:00 CST

- 修改人：codex
- 针对问题：OCR 首轮验证已有 15 份单页电子发票 PDF，但缺少可重复生成的图片样本、文本基线、样本清单和人工标注模板。
- 修改内容：新增 `scripts/prepare-ocr-dataset.sh` 与 `npm run ocr:prepare`，校验依赖和单页 PDF 后提取布局文本、按 200 DPI 渲染 PNG、计算 SHA-256，并原子生成 `manifest.tsv` 和 `labels.tsv`；新增 `doc/ocr-benchmark.md` 使用说明；已在 `debug/ocr-dataset/` 成功生成 15 张图片、15 份文本基线及两份 TSV，类型检查通过。

## 2026-07-18 15:19:02 CST

- 修改人：codex
- 针对问题：OCR 数据集的 `labels.tsv` 尚未填写，且重复执行准备脚本会覆盖后续人工核对结果。
- 修改内容：依据 15 份 PDF 文本层逐项填写发票号码、开票日期、价税合计、税额和销售方；逐字段验证所有值均存在于对应文本基线；调整准备脚本，仅在标注表不存在时创建空模板，重复执行不再覆盖已标注内容；同步更新 OCR 验证说明。

## 2026-07-18 15:21:46 CST

- 修改人：codex
- 针对问题：OCR 标注表缺少发票“金额（不含税）”，金额字段定义不完整。
- 修改内容：将标注金额字段明确拆分为 `amount`（不含税金额）、`total_amount`（价税合计）和 `tax_amount`（税额），补齐 15 份发票的不含税金额；同步更新空模板和说明文档；验证全部金额均存在于文本基线且 15 组数据均满足“金额＋税额＝总额”。

## 2026-07-18 16:11:52 CST

- 修改人：codex
- 针对问题：OCR 标签核对完成后，缺少可重复运行的候选模型、字段提取规则、原始结果及准确率与耗时统计。
- 修改内容：在独立 `invoice-ocr` Conda 环境中接入 PaddleOCR 3.7.0 与 ONNX Runtime，采用 PP-OCRv6 small 检测和识别模型；新增单次加载后批量运行 15 张样本的基准脚本，提取发票号码、日期、金额、总额、税额和销售方，生成逐字段预测、原始 JSON、准确率及耗时报告；新增 npm 命令、环境说明并排除 30 MB 模型缓存。

## 2026-07-18 16:32:02 CST

- 修改人：codex
- 针对问题：首轮 OCR 基准只测试 PP-OCRv6 small，缺少用户指定的 tiny 轻量规格对照。
- 修改内容：将基准脚本参数化为 tiny 和 small 两种规格，分别使用对应检测与识别模型并隔离结果目录；兼容 tiny 将“(小写)￥”与总额拆成相邻文本块的输出；新增模型准确率、速度、加载耗时和 ONNX 体积对比报告、npm 命令及使用说明。

## 2026-07-18 16:43:18 CST

- 修改人：codex
- 针对问题：PP-OCRv6 tiny 与 small 属于同一代模型，轻量 OCR 选型缺少独立代际的对照候选。
- 修改内容：在相同数据集、字段解析、CPU 和 ONNX Runtime 条件下新增 PP-OCRv5 mobile 候选；扩展基准脚本、npm 命令、模型体积统计和三模型汇总报告，结果与现有候选分别保存。

## 2026-07-18 17:43:24 CST

- 修改人：codex
- 针对问题：OCR 已完成真实样本选型，但尚未集成应用；用户只要求识别发票金额，并且正式 Windows 应用不能依赖开发机 Conda 环境。
- 修改内容：将 PP-OCRv6 tiny 检测、识别模型、6904 字符字典和 ONNX Runtime WASM 作为离线资源打包；新增 SHA-256 启动校验、安全本地资源协议、PDF 文本层优先和扫描/图片 OCR 回退；OCR 在单 Worker 中串行执行并于空闲 5 分钟释放 Session，只提取“价税合计（小写）”；添加发票时仅为空金额且首次关联发票的明细自动预填，已有金额不覆盖，失败仍保留附件并提示手工填写；新增金额解析测试，浏览器 Worker 冒烟测试实际识别样本 001 为 5600 分。

## 2026-07-18 17:54:30 CST

- 修改人：codex
- 针对问题：OCR 金额定义不完整，只填写价税合计且税额固定为零；上传发票时已有金额不会询问是否覆盖。
- 修改内容：OCR 改为按版面列位置分别提取金额（不含税）、税额和价税合计，并校验“金额＋税额＝价税合计”；PDF 文本层和图片 Worker 统一返回三项金额，多张发票分别汇总后填写价格与税费；明细已有金额时显示 Fluent 覆盖确认框，用户可对比三项识别值后选择覆盖或保留；同步更新测试、README、OCR 说明及需求文档，12 项测试和生产构建通过。

## 2026-07-18 18:14:18 CST

- 修改人：codex
- 针对问题：总价核算面板固定占用右侧宽度，挤压报销明细表的显示空间。
- 修改内容：总价核算改为默认收起的可切换侧栏；收起时彻底移除右侧网格列并在表格标题区提供“展开核算”入口，展开后在核算标题旁提供“收起”按钮，使表格恢复占用完整工作区宽度；12 项测试和生产构建通过。

## 2026-07-18 18:17:08 CST

- 修改人：codex
- 针对问题：总价核算展开后仍以右侧栏形式挤占表格空间，期望改为页面中央弹出并模糊背景。
- 修改内容：删除可展开右侧栏及双列工作区布局，将“展开核算”改为打开居中的 Fluent 模态面板；核算指标使用双列布局，类别与付款人合计并排显示，面板支持按钮、遮罩点击和 Esc 关闭；遮罩增加 10px 背景模糊，表格始终占满工作区；12 项测试和生产构建通过。

## 2026-07-18 18:24:42 CST

- 修改人：codex
- 针对问题：发票和支付截图单元格内同时放置“添加”、数量及空状态，操作拥挤；明细删除按钮文字占用空间。
- 修改内容：发票与支付截图单元格改为覆盖整格的单一 Fluent 点击区域并始终显示附件数量；无附件时点击直接添加，已有附件时点击进入管理窗口以继续添加、打开或删除，只读项目仍可查看已有附件；两列宽度同步收紧，明细删除按钮改为带无障碍名称和提示的红色“×”；12 项测试和生产构建通过。

## 2026-07-18 18:28:24 CST

- 修改人：codex
- 针对问题：发票、支付截图和删除三列虽已简化交互，但纯文字整格按钮层级单调，视觉效果仍不协调。
- 修改内容：将三列组合成带二级表头的“附件”视觉分组；发票和支付截图使用居中的圆角状态胶囊，分别以 Fluent 蓝、成功绿区分类别，圆形状态标识显示附件类型或添加符号，整格点击行为保持不变；删除“×”改为默认中性低对比，仅在当前行悬停、键盘聚焦或按钮悬停时增强，并进一步收紧三列宽度；12 项测试和生产构建通过。

## 2026-07-18 18:29:34 CST

- 修改人：codex
- 针对问题：明细删除按钮直接使用文字“×”字形且采用圆形红色样式，与附件列的 Fluent 视觉不协调。
- 修改内容：删除按钮改为 CSS 绘制的 12px 细线交叉图标，使用 6px 圆角中性按钮并降低默认对比度；仅在行悬停或键盘聚焦时显示清晰，按钮悬停时提供轻量浅红背景和边框反馈，同时保留无障碍名称与提示；12 项测试和生产构建通过。

## 2026-07-18 18:31:28 CST

- 修改人：codex
- 针对问题：删除按钮实际尺寸为 28×28px，与 48px 表格行高和 48px 操作列宽不匹配。
- 修改内容：删除按钮改为宽度 100%、高度 47px，完整覆盖扣除表格边框后的 48×48px 操作单元格；移除内部圆角和独立边框，使其与相邻附件整格按钮保持相同尺寸和边界，保留细线“×”及悬停反馈；12 项测试和生产构建通过。

## 2026-07-18 18:34:17 CST

- 修改人：codex
- 针对问题：全单元格删除按钮仍受 Fluent 小尺寸按钮内部布局影响，视觉尺寸与表格不匹配且交叉图标未准确居中。
- 修改内容：移除删除操作的 Fluent Button，改用尺寸完全受控的原生图标按钮；操作列设为 52px，单元格采用上下 7px、左右 9px 对称内边距，内部按钮固定 32×32px；使用 16×16 视口的 SVG 路径绘制 14px 交叉图标，并通过 CSS Grid 双轴居中，补充悬停、键盘焦点和禁用状态；12 项测试和生产构建通过。

## 2026-07-18 18:49:57 CST

- 修改人：codex
- 针对问题：项目缺少完整的 GitHub 上传忽略规则，调试发票、报销项目数据、OCR 样本、构建产物和本地工具状态可能被提交。
- 修改内容：重写根目录 `.gitignore`，排除依赖目录、前后端构建产物、Windows/Linux 安装包、调试目录、`*.invoice-project` 项目数据、私有 OCR 数据、编辑器与代理状态、环境变量、日志、覆盖率、Python 缓存及系统元数据；保留应用源码、Electron 主进程、测试、文档、脚本、`package-lock.json` 和正式运行所需的 `public/ocr/` 离线模型资源。

## 2026-07-18 18:52:51 CST

- 修改人：codex
- 针对问题：用户将 GitHub 上传范围限定为主要应用代码，明确要求测试代码也不上传。
- 修改内容：将 `.gitignore` 中仅忽略私有 OCR 测试数据的规则扩大为忽略整个 `tests/` 目录，测试代码和测试数据均不再纳入 Git 提交范围。
